"use strict";

window.LITTLE_ALCHEMIST_CONFIG = {
  // 完整網站部署到 Vercel 時維持相對路徑即可。
  // 若 GitHub Pages 要連到獨立部署的 API，改成完整網址：
  // aiEndpoint: "https://your-project.vercel.app/api/generate"
  aiEndpoint: "./api/generate"
};
